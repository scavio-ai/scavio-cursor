{
  "mcpServers": {
    "scavio": {
      "url": "https://mcp.scavio.dev/mcp",
      "headers": {
        "x-api-key": "${env:SCAVIO_API_KEY}",
        "x-scavio-platforms": "${env:SCAVIO_PLATFORMS}"
      }
    }
  }
}
